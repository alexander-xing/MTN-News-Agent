name: Weekly MTN Intelligence

on:
  schedule:
    # 周一 09:30 SAST (南非时间) = 07:30 UTC
    - cron: '30 7 * * 1'
  workflow_dispatch: 

jobs:
  run-agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' # 升级到 3.10，解决 Google SDK 不兼容问题

      - name: Install dependencies
        run: |
          pip install feedparser requests pytz deep-translator google-genai # 使用最新的 google-genai

      - name: Execute Agent
        env:
          EMAIL_ADDRESS: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          RECEIVER_EMAIL: ${{ secrets.RECEIVER_EMAIL }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python mtn.py
